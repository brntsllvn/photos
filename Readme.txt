To run this project you will need to initiate a NuGet package restore operation after opening the project in order to build and run them.

In Visual Studio under NuGet Packakage Manager - > Package Manager Setting make sure the following a checked.

 1) Visual Studio is configured to 'Allow NuGet to download missing packages'
 2) Visual Studio is configured to 'Automatically check for missing packages during build in Visual Studio'